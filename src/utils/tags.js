export const tagSlug = (value = '') =>
  value
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');

export const tagPath = (value = '') => `/tags/${tagSlug(value)}/`;
